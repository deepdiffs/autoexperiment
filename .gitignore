# Python
__pycache__/
*.py[oc]
build/
dist/
*.egg-info

# Virtual environments
.venv

# Agent artifacts
CLAUDE.md
AGENTS.md

# Transient per-run stdout — regenerated on every `uv run experiment.py`.
# results.tsv and progress.log are untracked during the experiment loop but
# committed as reference artifacts when a run concludes (see `viz.py`).
run.log
