# Python
__pycache__/
*.py[oc]
build/
dist/
*.egg-info

# Virtual environments
.venv

# Agent artifacts
CLAUDE.md
AGENTS.md

# Transient per-run stdout — regenerated on every `uv run experiment.py`.
# results.tsv and progress.log are committed on completed experiment branches
# as reference datasets (the viz tool reads them).
run.log
