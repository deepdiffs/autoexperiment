[project]
name = "autoexperiment"
version = "0.1.0"
description = "Benchmark tokens/sec of Gemma models served by a remote llama.cpp + LiteLLM endpoint"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]
